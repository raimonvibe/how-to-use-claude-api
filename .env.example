# Not used by the web app itself — the page asks for your key in the browser.
# This is here for the code samples in the guide, so you can run them locally.
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
